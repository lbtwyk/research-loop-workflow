# Codex Focus Path

Core ledger state lives at `.research-loop/execution-focus.json` inside the
worktree Git directory.

Older Codex checkouts used `codex/execution-focus.json`. If a worktree still
has that file, copy it to the new path once. Do not keep two focus files.
