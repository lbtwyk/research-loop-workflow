# Minimal core-only project

This directory is produced by `./install.sh --dest examples/minimal-project --core`.
It has one demo experiment and generated `ACTIVE.md` / `INDEX.md`.

```bash
python scripts/experiment_ledger.py outline EXP-20260902-demo
python scripts/experiment_ledger.py lint
```
